<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.hendi</groupId>
	<artifactId>CMS-Hendi</artifactId>
	<packaging>war</packaging>
	<version>1.0</version>
	<!-- <name>CMS</name> -->
	<description>JAVA CMS Template</description>

	<url>http://maven.apache.org</url>
	<properties>
		<!-- UTF-8 Encoding -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

		<!-- Java -->
		<maven.compiler.release>25</maven.compiler.release>

		<!-- Maven Plugin -->
		<maven-compiler-plugin.version>3.16.0</maven-compiler-plugin.version>
		<maven-surefire-plugin.version>3.6.0</maven-surefire-plugin.version>
		<maven-war-plugin.version>3.5.1</maven-war-plugin.version>
		<jetty-maven-plugin.version>12.1.13</jetty-maven-plugin.version>

		<!-- Logging -->
		<org.slf4j.version>2.0.19</org.slf4j.version>
		<ch.qos.logback.version>1.6.3</ch.qos.logback.version>

		<!-- Apache Commons -->
		<commons-lang.version>3.20.0</commons-lang.version>

		<!-- Hibernate ORM -->
		<org.hibernate.version>7.4.8.Final</org.hibernate.version>

		<!-- Jackson -->
		<com.fasterxml.jackson.core.version>2.22.2</com.fasterxml.jackson.core.version>

		<!-- MySQL Connector -->
		<mysql-connector.version>26.7.0</mysql-connector.version>

		<!-- Jakarta EE -->
		<jakarta.annotation.version>3.0.0</jakarta.annotation.version>
		<jakarta.servlet.version>6.1.0</jakarta.servlet.version>
		<jakarta.servlet.jsp.jstl.version>3.0.2</jakarta.servlet.jsp.jstl.version>
		<org.glassfish.web.jstl.version>3.0.1</org.glassfish.web.jstl.version>

		<!-- Sitemesh -->
		<org.sitemesh.version>3.2.3</org.sitemesh.version>

		<!-- Spring Framework -->
		<org.springframework.version>7.0.9</org.springframework.version>

		<!-- Spring Security -->
		<org.springframework.security.version>7.1.1</org.springframework.security.version>

		<!-- Test -->
		<org.junit.jupiter.version>6.1.3</org.junit.jupiter.version>
	</properties>

	<dependencies>
		<!-- JUnit -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${org.junit.jupiter.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- Logging -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${org.slf4j.version}</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>${ch.qos.logback.version}</version>
		</dependency>

		<!-- Apache Commons -->
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
			<version>${commons-lang.version}</version>
		</dependency>

		<!-- Hibernate ORM -->
		<dependency>
			<groupId>org.hibernate</groupId>
			<artifactId>hibernate-core</artifactId>
			<version>${org.hibernate.version}</version>
		</dependency>
		<dependency>
			<groupId>org.hibernate</groupId>
			<artifactId>hibernate-c3p0</artifactId>
			<version>${org.hibernate.version}</version>
		</dependency>

		<!-- Jackson -->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>${com.fasterxml.jackson.core.version}</version>
		</dependency>

		<!-- MySQL Connector -->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<version>${mysql-connector.version}</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Jakarta EE -->
		<dependency>
			<groupId>jakarta.annotation</groupId>
			<artifactId>jakarta.annotation-api</artifactId>
			<version>${jakarta.annotation.version}</version>
		</dependency>
		<dependency>
			<groupId>jakarta.servlet</groupId>
			<artifactId>jakarta.servlet-api</artifactId>
			<version>${jakarta.servlet.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>jakarta.servlet.jsp.jstl</groupId>
			<artifactId>jakarta.servlet.jsp.jstl-api</artifactId>
			<version>${jakarta.servlet.jsp.jstl.version}</version>
		</dependency>
		<dependency>
			<groupId>org.glassfish.web</groupId>
			<artifactId>jakarta.servlet.jsp.jstl</artifactId>
			<version>${org.glassfish.web.jstl.version}</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Sitemesh -->
		<dependency>
			<groupId>org.sitemesh</groupId>
			<artifactId>sitemesh</artifactId>
			<version>${org.sitemesh.version}</version>
		</dependency>

		<!-- Spring Framework -->
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
			<version>${org.springframework.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-orm</artifactId>
			<version>${org.springframework.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-webmvc</artifactId>
			<version>${org.springframework.version}</version>
		</dependency>

		<!-- Spring Security -->
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-config</artifactId>
			<version>${org.springframework.security.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-web</artifactId>
			<version>${org.springframework.security.version}</version>
		</dependency>
	</dependencies>

	<build>
<!-- 		<finalName>${project-artifactId}-${project.version}</finalName> -->
		<finalName>CMS-Hendi-${project.version}</finalName>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<excludes>
					<exclude>application.sql</exclude>
					<exclude>logback.xml</exclude>
				</excludes>
			</resource>
			<!-- logback.xml uses ${...} for its own substitutions, so keep it unfiltered -->
			<resource>
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
				<includes>
					<include>logback.xml</include>
				</includes>
			</resource>
		</resources>

		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven-compiler-plugin.version}</version>
				<configuration>
					<release>${maven.compiler.release}</release>
					<compilerArgs>
						<!-- Retain parameter names for Spring's constructor/argument binding -->
						<arg>-parameters</arg>
					</compilerArgs>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven-surefire-plugin.version}</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>${maven-war-plugin.version}</version>
				<configuration>
					<archive>
						<manifest>
							<addClasspath>true</addClasspath>
						</manifest>
					</archive>
				</configuration>
			</plugin>

			<!-- Jetty Server -->
			<plugin>
				<groupId>org.eclipse.jetty.ee11</groupId>
				<artifactId>jetty-ee11-maven-plugin</artifactId>
				<version>${jetty-maven-plugin.version}</version>
				<configuration>
					<scan>10</scan>
					<httpConnector>
						<port>8282</port>
						<idleTimeout>10000</idleTimeout>
					</httpConnector>
					<webApp>
						<contextPath>/CMS-Hendi</contextPath>
					</webApp>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
